import React from 'react';
import { createRoot } from 'react-dom/client';
import { SupportDialog } from '../../src/components/support/SupportDialog';
createRoot(document.getElementById('root')!).render(<SupportDialog />);
